<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ankit.imgshare2android" >

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AuthActivity"
            android:label="@string/title_activity_auth" >
        </activity>
        <activity
            android:name=".AuthHandler"
            android:label="@string/title_activity_auth_handler" >
        </activity>
        <activity
            android:name=".ManageActivity"
            android:label="@string/title_activity_manage" >
        </activity>
        <activity
            android:name=".ViewAllStreamsActivity"
            android:label="@string/title_activity_view_all_streams" >
        </activity>
        <activity
            android:name=".SampleGridViewActivity"
            android:label="@string/title_activity_sample_grid_view" >
        </activity>

        <!--<activity android:name=".SampleContactsActivity"/>-->
        <activity android:name=".SampleGalleryActivity"/>
        <!--<activity android:name=".SampleListDetailActivity"/>-->

    </application>

</manifest>
